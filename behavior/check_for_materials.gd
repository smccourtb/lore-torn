extends BTConditional


func _pre_tick(agent: Node, _blackboard: Blackboard) -> void:
#	verified = false	
#	for jobs in agent.data.assigned_jobs:
#		if jobs == "chop":
# TODO: Implement checking stockpiles for items
# loop through all stockpiles items array
# keep going until you find enough
# get the projects materials array

	verified = true
